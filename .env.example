# Copy to .env and add your key, OR export it in your shell before `npm start`.
# Get a free key at https://aistudio.google.com/apikey  →  never commit the real value.
GEMINI_API_KEY=...
# Optional: change the port (default 8123)
# PORT=8123
