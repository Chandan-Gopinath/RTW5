# Copy to .env and add your keys, OR export them in your shell before `npm start`.
# Never commit real values.

# --- grading models ---
# Gemini: https://aistudio.google.com/apikey
GEMINI_API_KEY=...
# Groq (free, no card): https://console.groq.com
GROQ_API_KEY=...

# --- accounts / auth (account foundation) ---
DATABASE_URL=postgres://...            # Neon pooled connection string
BREVO_API_KEY=...                      # Brevo transactional API key
BREVO_SENDER=noreply@yourdomain.com    # verified Brevo sender address
ADMIN_EMAILS=you@example.com           # comma-separated admin allowlist
APP_URL=https://you-got-it.vercel.app  # absolute base for magic-link URLs
SESSION_DAYS=30                        # optional session length (days)

# --- reminder engine (daily nudge + weekly recap crons) ---
CRON_SECRET=...                        # long random string; Vercel sends it as a
                                       # bearer header to /api/cron/*. Set the SAME
                                       # value in Vercel + here to run crons locally.
DEV_ECHO_LINK=1                        # dev only: log the magic link instead of emailing

# Optional: change the port (default 8123)
# PORT=8123
